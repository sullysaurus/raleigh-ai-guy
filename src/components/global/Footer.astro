---
import Text from "@/components/fundations/elements/Text.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
const navLinks = [
  {
    text: "Home",
    href: "/",
    classes: "hover:text-black duration-300 dark:hover:text-white/20",
  },
  {
    text: "Blog",
    href: "/blog/",
    classes: "hover:text-black duration-300",
  },
  {
    text: "Projects",
    href: "/projects/",
    classes: "hover:text-black duration-300",
  },
  {
    text: "Work with me",
    href: "/blog/posts/intro",
    classes: "hover:text-black duration-300",
  },
  {
    text: "LinkedIn",
    href: "https://www.linkedin.com/in/danny-sullivan-raleigh/",
    classes: "hover:text-black duration-300",
  },
];
// Split the navLinks array into two columns
const navLinksByColumn = [
  navLinks.slice(Math.ceil(navLinks.length / 2)), // First column
  navLinks.slice(0, Math.ceil(navLinks.length / 2)), // Second column
];
---

<footer>
  <Wrapper variant="standard" class="py-12">
    <div class="grid grid-cols-1 gap-12 lg:grid-cols-3">
      {
        navLinksByColumn.map((columnLinks) => (
          <nav class="flex flex-col gap-y-2 text-base-500 dark:text-base-500">
            {columnLinks.map((link) => (
              <Text
                tag="a"
                variant="textSM"
                href={link.href}
                title={link.title}
                aria-label={link.ariaLabel}
                class="text-base-500 hover:text-black dark:text-base-400 dark:hover:text-white font-medium"
              >
                {link.text}
              </Text>
            ))}
          </nav>
        ))
      }
    </div>
  </Wrapper>
</footer>
