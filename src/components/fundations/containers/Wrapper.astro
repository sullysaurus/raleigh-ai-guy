---
interface Props {
  variant?: "standard" | "wide" | "prose"; // Define any variants you need
  class?: string; // Optional prop for additional classes
}
const { variant = "standard", class: extraClass = "" } = Astro.props;
// Map each variant to its specific classes
const variantClasses = {
  // All purpose wrapper
  standard: "max-w-3xl mx-auto px-6 sm:px-8 w-full",
  wide: "max-w-6xl mx-auto px-6 sm:px-8 lg:px-10 w-full",
  // Wrapper for prose blocks
  prose:
    "prose-ul:text-base-500 prose max-w-3xl pt-0 mx-auto prose-a:text-black prose-p:text-base-500 prose-code:text-black prose-strong:text-black prose-ol:text-base-500 prose-headings:text-black prose-headings:text-base text-sm",
};
// Combine the classes for the specified variant with any extra classes
const classes = `${variantClasses[variant]} ${extraClass}`.trim();
---

<div class={classes}>
  <slot />
</div>
